import React from 'react'

export default function ProblemModal({selectedProblem,modifyProblemModal}) {
    "Displays the fields like computer id add date and the problem and adds it by simple post"

    return (
        modifyProblemModal && 
            <UniModal>

        </UniModal>
    )
}
